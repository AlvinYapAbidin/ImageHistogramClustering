cmake_minimum_required(VERSION 3.5)
project(photohist)
find_package(OpenCV REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS})
add_executable(photohist main.cpp photohist.cpp)
target_link_libraries(photohist ${OpenCV_LIBS})